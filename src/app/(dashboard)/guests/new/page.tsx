import { GuestForm } from "@/components/forms/guest-form";

export const metadata = {
  title: "Register New Guest - GrandHotel",
};

export default function NewGuestPage() {
  return <GuestForm />;
}
