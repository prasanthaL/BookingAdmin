import { AddonForm } from "@/components/forms/addon-form";

export const metadata = {
  title: "Create New Service - GrandHotel",
};

export default function NewAddonPage() {
  return <AddonForm />;
}
